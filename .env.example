# Environment Configuration
NODE_ENV=development

# Server Configuration
SERVER_PORT=3001
HTTP_PORT=80
HTTPS_PORT=443
VITE_DEV_PORT=5173

# CORS Configuration
CORS_ORIGIN=http://localhost

# Database Configuration (MySQL/MariaDB)
DB_NAME=starter
DB_USER=starter_user
DB_PASSWORD=password
DB_ROOT_PASSWORD=rootpassword
DATABASE_URL=mysql://starter_user:password@database:3306/starter

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend Configuration
VITE_API_URL=/api

# ============================================================================
# LOCAL DEVELOPMENT ONLY
# ============================================================================
# This .env file is ONLY used for local Docker Compose development
# For App Platform deployment, ALL variables must be set in DO Console UI
# (.env files are NOT uploaded to App Platform)

# Admin Credentials (Local Development)
# For local development, seed.ts uses these defaults:
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=AdminPassword123!

# ============================================================================
# APP PLATFORM DEPLOYMENT - DO NOT USE THIS FILE
# ============================================================================
# Set ALL these variables in Digital Ocean Console UI (Settings > Environment Variables)
# at APP LEVEL (not service level) with "Encrypt" checked for sensitive values:
#
# CORS_ORIGIN = https://yourapp.ondigitalocean.app
# VITE_API_URL = /api
# DB_NAME = starter
# DB_USER = starter_user
# SERVER_PORT = 3001
# DB_URL = ********** [ENCRYPT]
# JWT_SECRET = ********** [ENCRYPT, 32+ chars]
# ADMIN_EMAIL = ********** [ENCRYPT]
# ADMIN_PASSWORD = ********** [ENCRYPT]
# SEED_DATABASE = true